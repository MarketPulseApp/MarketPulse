global:
        scrape_interval: 15s
      scrape_configs:
        - job_name: "marketpulse-api"
          static_configs:
            - targets: ["host.docker.internal:8080"]
        - job_name: "postgres"
          static_configs:
            - targets: ["postgres:9187"]  # postgres_exporter