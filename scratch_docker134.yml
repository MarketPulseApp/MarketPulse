services:
  api:
    build: ./app
    container_name: marketpulse-api
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
      - ./secrets:/opt/marketpulse/secrets

  worker:
    build:
      context: .
      dockerfile: ./workers/Dockerfile
    container_name: marketpulse-worker
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
      - ./secrets:/opt/marketpulse/secrets

  bot:
    build: ./bot
    container_name: marketpulse-bot
    restart: unless-stopped
    env_file: .env

  node-exporter:
    image: prom/node-exporter:latest
    container_name: marketpulse-node-exporter
    restart: unless-stopped
    pid: host
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    ports:
      - "9100:9100"

  promtail:
    image: grafana/promtail:latest
    container_name: marketpulse-promtail
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - ./promtail/config.yml:/etc/promtail/config.yml:ro
    command: -config.file=/etc/promtail/config.yml

  jupyter:
    image: jupyter/minimal-notebook:latest
    container_name: marketpulse-jupyter
    restart: unless-stopped
    ports:
      - "8888:8888"
    volumes:
      - ./data:/home/jovyan/data
    environment:
      - JUPYTER_TOKEN=marketpulse_password_1122
